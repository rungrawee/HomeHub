# Frontend

This directory is reserved for the web interface.

The frontend will call the backend API instead of connecting directly to the
database. This keeps database credentials and business rules on the server.
